import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{Header}from"../../components/Header";import{Footer}from"../../components/Footer";import{attractionGuideByCanonicalSlug,attractionCanonicalSlugByLegacy,canonicalizeAttractionHref}from"../../../data/attraction-guides";
export function generateStaticParams(){return Object.values(attractionCanonicalSlugByLegacy).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const g=attractionGuideByCanonicalSlug[slug];return g?{title:g.title,description:g.description,alternates:{canonical:`/how-to-attract/${slug}`}}:{}}
export default async function AttractionGuidePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=attractionGuideByCanonicalSlug[slug];if(!g)notFound();const faq=[{q:`What is the safest first step for ${g.title.toLowerCase()}?`,a:g.intro},{q:"How quickly should I expect birds to respond?",a:"There is no guaranteed timeline. Local range, season, weather, natural food, habitat, disturbance, and discovery time all affect visits."}];return <div><Header/><main className="content-main"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"Article",headline:g.title,description:g.description,mainEntityOfPage:`https://attractbirds.app/how-to-attract/${slug}`})}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"FAQPage",mainEntity:faq.map(x=>({"@type":"Question",name:x.q,acceptedAnswer:{"@type":"Answer",text:x.a}}))})}}/><div className="breadcrumb"><Link href="/">Home</Link> / <Link href="/how-to-attract-birds">How to attract birds</Link> / {g.title}</div><section className="content-hero"><div><p className="eyebrow"><span/> {g.eyebrow}</p><h1>{g.title}</h1><p className="lede">{g.intro}</p></div><aside className="fact-panel">{g.facts.map(([label,value])=><div key={label}><small>{label}</small><strong>{value}</strong></div>)}</aside></section><div className="content-grid">{g.sections.map((s,i)=><section className="info-block" key={s.title}><small className="step-number">0{i+1}</small><h2>{s.title}</h2><p>{s.body}</p><ul>{s.items.map(x=><li key={x}>{x}</li>)}</ul></section>)}{g.warning&&<section className="info-block wide safety-note"><h2>{g.warning.title}</h2><p>{g.warning.body}</p></section>}<section className="info-block wide"><h2>Continue planning</h2><div className="chip-list">{g.related.map(x=><Link href={canonicalizeAttractionHref(x.href)} key={x.href}>{x.label}</Link>)}<Link href="/how-to-attract-birds">All attraction guides</Link></div></section><section className="info-block wide"><h2>Frequently asked questions</h2>{faq.map(x=><div key={x.q}><h3>{x.q}</h3><p>{x.a}</p></div>)}</section></div></main><Footer/></div>}
