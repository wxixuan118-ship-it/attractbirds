import type{Metadata}from"next";import{notFound}from"next/navigation";import{Header}from"../../../components/Header";import{Footer}from"../../../components/Footer";import{EditorialPage,editorialMetadata}from"../../../components/Editorial";import{PlantLinks}from"../../../components/PlantLinks";import{plantEditorial}from"../../../../data/editorial/plants";import{getBirdPlantGuide,getBirdPlantStaticParams}from"../../../../lib/plant-repository";
export function generateStaticParams(){return getBirdPlantStaticParams()}
export async function generateMetadata({params}:{params:Promise<{bird:string}>}):Promise<Metadata>{const{bird}=await params;const ed=plantEditorial[`/plants/for/${bird}`];if(ed)return editorialMetadata(ed);const d=getBirdPlantGuide(bird);return d?{title:`Plants That Attract ${d.bird.commonName}s`,description:`Compare reviewed plants that provide food, insects, nectar, nesting structure, or shelter for ${d.bird.commonName}s.`,alternates:{canonical:`/plants/for/${bird}`}}:{}}
export default async function BirdPlants({params}:{params:Promise<{bird:string}>}){const{bird}=await params;const d=getBirdPlantGuide(bird);if(!d)notFound();const ed=plantEditorial[`/plants/for/${bird}`];if(ed)return <EditorialPage content={ed} eyebrow="Plant guide by bird" breadcrumbs={[{name:"Plants",path:"/plants"},{name:ed.h1??ed.title.split(":")[0],path:ed.path}]}><section className="loc-section"><div className="loc-section-header"><h2>Related guides</h2></div><div className="chip-list"><a href={`/birds/${d.bird.slug}`}>{d.bird.commonName} profile →</a><a href={`/feeders/for/${d.bird.slug}`}>Feeders for {d.bird.commonName.toLowerCase()}s →</a><a href={`/how-to-attract/${d.bird.slug}`}>How to attract {d.bird.commonName.toLowerCase()}s →</a></div></section><PlantLinks exclude={[ed.path]} highlight={d.plants.map((x)=>x.slug)}/></EditorialPage>;return <div><Header/><main className="content-main"><div className="breadcrumb"><a href="/">Home</a> / <a href="/plants">Plants</a> / For {d.bird.commonName}</div><section className="content-hero"><div><p className="eyebrow"><span/> Reviewed bird–plant guide</p><h1>Plants for {d.bird.commonName}s</h1><p className="lede">{d.bird.summary} The plants below match reviewed relationships or this species’ existing habitat guidance; verify local nativity before planting.</p></div></section><div className="content-grid"><section className="info-block wide"><h2>Recommended plant shortlist</h2>{d.plants.length?<div className="index-grid">{d.plants.map(p=><a className="index-card" href={`/plants/${p.slug}`} key={p.slug}><small>{p.scientificName}</small><h2>{p.commonName}</h2><p>{p.benefits.join(" · ")}</p><span>Compare care →</span></a>)}</div>:<p>Specific plant relationships are still under review. Start with locally native plants that provide the species’ natural food and protective cover.</p>}</section><section className="info-block"><h2>Why plants help</h2><p>Plants can supply natural food, host insects, create nesting structure, and provide escape or weather cover. A relationship does not mean the plant is suitable in every region.</p></section><section className="info-block"><h2>Complete the habitat</h2><p>Add clean water, reduce pesticides, prevent window collisions, and keep cats indoors.</p><a className="button button-primary" href={`/birds/${d.bird.slug}`}>Full bird profile →</a></section></div></main><Footer/></div>}
